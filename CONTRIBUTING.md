# Contributing to CNull
- Python 3.9+
- `pip install -e .`
- Run `pytest` before PRs
- Keep figures deterministic (set seeds)
- Add examples to `examples/`

## Dev quickstart
```
python -m venv .venv && source .venv/bin/activate
pip install -e .
pip install pytest
pytest
```
